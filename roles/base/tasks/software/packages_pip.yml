- name: system setup | pipx packages | install bpytop
  tags: bpytop,packages,pipx,python
  become_user: "{{ target_username }}"
  community.general.pipx:
    state: present
    name: bpytop
  ignore_errors: true
